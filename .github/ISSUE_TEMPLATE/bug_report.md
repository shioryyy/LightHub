---
name: Bug report
about: Something in LightHub does not work as documented
labels: bug
---

**Version / build** (from the window title, `BUILD-METADATA.json` or `--help`):

**Platform** (Windows / Linux / macOS, version):

**Connection** (receiver model, wired, ...):

**What happened**:

**What did you expect**:

**Steps to reproduce**:

**Diagnostics** (optional but very helpful): Help → Export diagnostics. The export
is redacted: it omits unit IDs, raw memory, paths and user mappings. Do not attach
backups (`.lhbackup`) publicly — they contain device identity and mappings.

**Logs**: `application.log` under the OS local application-data `LightHub` directory
may contain failure kinds; attach only if relevant.
